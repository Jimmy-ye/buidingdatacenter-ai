# BDC-AI PC 端应用

## 🖥️ 项目概述

基于 NiceGUI 开发的 BDC-AI 平台 PC 端应用，提供工程结构浏览、Asset 管理、数据可视化等功能。

### 核心功能

- ✅ 工程结构树浏览（Building/Zone/System/Device）
- ✅ Asset 资产列表展示（图片、表格、文档）
- ✅ 图片上传与 OCR 结果查看
- ✅ GLM-4V 场景问题报告展示
- ✅ 数据统计与可视化
- ✅ 多维度资产查询与过滤

---

## 🛠️ 技术栈

- **NiceGUI**: 2.7.0
- **FastAPI**: 0.104.1（与现有后端集成）
- **Python**: 3.11+
- **Vue.js**: 3.x（NiceGUI 内置）
- **Plotly**: 数据可视化（可选）

---

## 📁 项目结构

```
desktop/
├── nicegui_app/              # NiceGUI 主应用
│   ├── main.py               # 应用入口
│   ├── pages/                # 页面组件
│   │   ├── home.py           # 首页
│   │   ├── engineering.py    # 工程结构页面
│   │   ├── assets.py         # 资产列表页面
│   │   └── analysis.py       # 数据分析页面
│   ├── components/           # 可复用组件
│   │   ├── tree_view.py      # 工程结构树组件
│   │   ├── asset_table.py    # 资产表格组件
│   │   └── image_viewer.py   # 图片查看器组件
│   ├── services/             # 服务层
│   │   ├── api_service.py    # API 调用服务
│   │   └── data_service.py   # 数据处理服务
│   ├── utils/                # 工具类
│   │   └── helpers.py
│   └── requirements.txt      # Python 依赖
└── README.md                 # 本文件
```

---

## 🚀 快速开始

### 环境要求

- Python 3.11+
- FastAPI 0.104.1+
- 现有后端服务运行在 http://localhost:8000

### 安装依赖

```bash
cd desktop/nicegui_app
pip install -r requirements.txt
```

### 启动应用

```bash
# 开发模式（带热重载）
python main.py --reload

# 生产模式
python main.py
```

应用将在 `http://localhost:8080` 启动。

---

## 🔧 配置说明

### 后端 API 配置

在 `services/api_service.py` 中配置：

```python
class ApiService:
    BASE_URL = "http://localhost:8000/api/v1"
    TIMEOUT = 30  # 秒
```

### 应用配置

在 `main.py` 中配置：

```python
from nicegui import ui

ui.run(
    title="BDC-AI 建筑节能管理平台",
    port=8080,
    host="localhost",
    dark=True,              # 暗色主题
    reconnect_interval=5,   # 自动重连
    show_welcome_message=False,
)
```

---

## 📊 页面设计

### 1. 首页（Home）

```
┌─────────────────────────────────────────────────────────┐
│  BDC-AI 建筑节能管理平台                                 │
├─────────────────────────────────────────────────────────┤
│  [工程结构]  [资产管理]  [数据分析]  [系统设置]          │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  项目概览                                                 │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐                 │
│  │项目数量  │  │建筑数量  │  │设备数量  │                 │
│  │   12    │  │   45    │  │  1234   │                 │
│  └─────────┘  └─────────┘  └─────────┘                 │
│                                                           │
│  最近上传的 Asset                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 图片名称    | 类型    | 时间        | 操作       │   │
│  │ 电表照片    | image   | 2026-01-20  | [查看]     │   │
│  │ 现场问题    | image   | 2026-01-19  | [查看]     │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 2. 工程结构页面（Engineering）

```
┌─────────────────────────────────────────────────────────┐
│  工程结构                                                │
├──────────┬──────────────────────────────────────────────┤
│          │                                               │
│ 工程树   │  设备列表                                     │
│ ▼ 示例项目│  ┌────────────────────────────────────────┐ │
│   ├ A座  │  │ 设备名称    | 类型    | 状态    | 操作  │ │
│   │ ├5F  │  │ FCU-03     | FCU     | 正常    | [查看]│ │
│   │ └4F  │  │ FCU-04     | FCU     | 维修中  | [查看]│ │
│   └ B座  │  │ 冷机-01    | Chiller | 正常    | [查看]│ │
│          │  └────────────────────────────────────────┘ │
└──────────┴──────────────────────────────────────────────┘
```

### 3. 资产列表页面（Assets）

```
┌─────────────────────────────────────────────────────────┐
│  资产管理                                                │
├─────────────────────────────────────────────────────────┤
│  [上传图片] [刷新] [导出]                                │
│  筛选: [项目▼] [建筑▼] [系统▼] [设备▼] [类型▼]         │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │ 预览        | 信息        | 时间        | 操作  │   │
│  │ [缩略图]    │ 电表照片    │ 2026-01-20  │ [查看]│   │
│  │             │ 设备: FCU-03 │             │ [编辑]│   │
│  │             │ OCR: 1234   │             │ [删除]│   │
│  ├─────────────────────────────────────────────────┤   │
│  │ [缩略图]    │ 现场问题    │ 2026-01-19  │ [查看]│   │
│  │             │ 设备: 冷却塔 │             │ [编辑]│   │
│  │             │ 问题: 漏水   │             │ [删除]│   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 4. 数据分析页面（Analysis）

```
┌─────────────────────────────────────────────────────────┐
│  数据分析                                                │
├─────────────────────────────────────────────────────────┤
│  [生成报告] [导出数据]                                   │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  能耗趋势图                                              │
│  ┌─────────────────────────────────────────────────┐   │
│  │  ▁▃▅▇█ 能耗 (kWh)                                │   │
│  │                                                   │   │
│  │  200 ┤      ╭──╮                                 │   │
│  │  150 ┤    ╭─╯  ╰╮                                │   │
│  │  100 ┤  ╭─╯      ╰──╮                            │   │
│  │   50 ┤╭─╯           ╰───╮                        │   │
│  │    0 ┼───────────────────────                     │   │
│  │       1月 2月 3月 4月 5月 6月                    │   │
│  └─────────────────────────────────────────────────┘   │
│                                                           │
│  问题统计                                                │
│  ┌──────────┬──────────┬──────────┐                    │
│  │ 高危     │ 中等     │ 低危     │                    │
│  │   12     │   34     │   56     │                    │
│  └──────────┴──────────┴──────────┘                    │
└─────────────────────────────────────────────────────────┘
```

---

## 🔌 API 集成

### 调用后端 API

```python
import httpx

class ApiService:
    BASE_URL = "http://localhost:8000/api/v1"

    async def get_assets(self, **filters):
        """获取资产列表"""
        async with httpx.AsyncClient() as client:
            response = await client.get(
                f"{self.BASE_URL}/assets/",
                params=filters,
                timeout=30
            )
            return response.json()

    async def upload_image(self, file_path, metadata):
        """上传图片"""
        async with httpx.AsyncClient() as client:
            with open(file_path, 'rb') as f:
                files = {'file': f}
                response = await client.post(
                    f"{self.BASE_URL}/assets/upload_image_with_note",
                    files=files,
                    data=metadata
                )
                return response.json()
```

---

## 🎨 UI 组件示例

### 工程结构树组件

```python
from nicegui import ui
import httpx

class EngineeringTree:
    def __init__(self, on_select=None):
        self.on_select = on_select
        self.tree = ui.tree([], on_select=self.handle_select)

    async def load_tree_data(self, project_id):
        """加载工程结构树"""
        async with httpx.AsyncClient() as client:
            response = await client.get(
                f"http://localhost:8000/api/v1/projects/{project_id}/structure_tree"
            )
            tree_data = response.json()
            self.tree.props('nodes', tree_data)

    def handle_select(self, event):
        """处理节点选择"""
        if self.on_select:
            self.on_select(event)
```

### 资产表格组件

```python
from nicegui import ui
import httpx

class AssetTable:
    def __init__(self):
        self.table = ui.table(
            columns=[
                {'name': 'id', 'label': 'ID', 'field': 'id'},
                {'name': 'modality', 'label': '类型', 'field': 'modality'},
                {'name': 'capture_time', 'label': '时间', 'field': 'capture_time'},
                {'name': 'engineer_path', 'label': '位置', 'field': 'engineer_path'},
            ],
            rows=[]
        )

    async def load_assets(self, **filters):
        """加载资产列表"""
        async with httpx.AsyncClient() as client:
            response = await client.get(
                "http://localhost:8000/api/v1/assets/",
                params=filters
            )
            assets = response.json()
            self.table.props('rows', assets)
```

---

## 📝 开发任务清单

### 阶段 1：项目初始化（0.5 天）

- [ ] 创建 NiceGUI 项目
- [ ] 目录结构搭建
- [ ] 依赖安装（nicegui, httpx）
- [ ] 基础路由配置
- [ ] 主题配置

### 阶段 2：首页与布局（0.5 天）

- [ ] 导航栏组件
- [ ] 侧边栏菜单
- [ ] 首页统计卡片
- [ ] 最近 Asset 列表

### 阶段 3：工程结构浏览（1 天）

- [ ] 工程结构树组件
- [ ] 树节点点击事件
- [ ] 设备列表展示
- [ ] 设备详情页面

### 阶段 4：Asset 管理（1 天）

- [ ] Asset 列表页面
- [ ] 图片上传对话框
- [ ] 图片预览组件
- [ ] OCR 结果展示
- [ ] 多维度筛选

### 阶段 5：数据分析（0.5 天）

- [ ] 数据统计页面
- [ ] 图表展示（Plotly）
- [ ] 报告导出

### 阶段 6：优化与测试（0.5 天）

- [ ] 响应式布局优化
- [ ] 加载状态提示
- [ ] 错误处理
- [ ] 性能优化

**总计**：约 4 天

---

## 🧪 测试

```bash
# 运行 NiceGUI 应用
python main.py

# 访问
http://localhost:8080
```

---

## 🐛 常见问题

### 1. 后端连接失败

确保后端服务运行在 `http://localhost:8000`：

```bash
cd services/backend
python -m uvicorn app.main:app --host localhost --port 8000 --reload
```

### 2. 依赖安装失败

```bash
pip install --upgrade pip
pip install nicegui httpx
```

### 3. 端口冲突

修改 `main.py` 中的端口：

```python
ui.run(port=8081)  # 改为其他端口
```

---

## 📖 参考资料

- [NiceGUI 官方文档](https://nicegui.io/)
- [NiceUI GitHub](https://github.com/zauberzeug/nicegui)
- [FastAPI 文档](https://fastapi.tiangolo.com/)

---

## 📮 联系方式

项目相关问题，请联系项目负责人。

---

**创建时间**: 2026-01-20
**开发者**: Jimmy Ye
**版本**: 0.1.0
