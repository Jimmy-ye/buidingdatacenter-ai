# 技术栈选型指南

> **文档版本**: v2.0
> **最后更新**: 2026-01-20
> **基于文档**: 项目规划.md

---

## 📋 目录

- [一、已采用技术栈](#一已采用技术栈)
- [二、后端与数据库技术](#二后端与数据库技术)
- [三、AI 与多模态处理](#三ai-与多模态处理)
- [四、前端与移动端技术](#四前端与移动端技术)
- [五、向量数据库与检索](#五向量数据库与检索)
- [六、推荐的扩展技术](#六推荐的扩展技术)

---

## 一、已采用技术栈

### ✅ MVP 阶段（已实现）

| 技术组件 | 版本 | 用途 | 状态 |
|---------|------|------|------|
| **Python** | 3.11+ | 开发语言 | ✅ |
| **FastAPI** | 0.104.1 | Web 框架 | ✅ |
| **SQLAlchemy** | 2.0.23 | ORM | ✅ |
| **PostgreSQL** | 18.1 | 关系数据库 | ✅ |
| **Uvicorn** | - | ASGI 服务器 | ✅ |
| **PaddleOCR** | 2.7.0.3 | OCR 引擎 | ✅ |
| **GLM-4V** | - | 多模态大模型 | ✅ |
| **bigtree** | 0.16.4 | 树形结构处理 | ✅ |
| **pytest** | - | 测试框架 | ✅ |

**核心优势**:
- ✅ 轻量快速开发
- ✅ 原生 UUID 支持
- ✅ 完善的类型检查
- ✅ 丰富的生态系统

---

## 二、后端与数据库技术

### 🔹 后端框架

#### FastAPI ⭐ 已采用（强烈推荐）

**官网**: https://fastapi.tiangolo.com/
**GitHub**: https://github.com/tiangolo/fastapi

**优势**:
- 高性能异步框架
- 自动生成 API 文档（Swagger/ReDoc）
- 类型提示和数据验证（Pydantic）
- 依赖注入系统

**适用场景**:
- ✅ RESTful API 服务
- ✅ 异步任务处理
- ✅ 微服务架构

---

### 🔹 数据库

#### PostgreSQL ⭐ 已采用（生产推荐）

**官网**: https://www.postgresql.org/
**版本**: 18.1

**核心优势**:
- 原生 UUID 支持
- JSONB 类型（灵活查询）
- 扩展生态（pgvector, TimescaleDB）
- ACID 事务保证

**已安装扩展**:
```sql
-- 查看已安装扩展
SELECT * FROM pg_extension;

-- 推荐安装
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";    -- UUID 生成
CREATE EXTENSION IF NOT EXISTS "pg_trgm";      -- 模糊搜索
```

#### TimescaleDB（时序数据扩展）⭐ 阶段 2 推荐

**官网**: https://www.timescale.com/
**GitHub**: https://github.com/timescale/timescaledb

**适用场景**:
- ✅ 传感器时序数据（能耗、温度）
- ✅ 自动分区和压缩
- ✅ 时间窗口查询优化

**安装**:
```bash
# Docker 方式
docker run -d --name timescaledb \
  -p 5432:5432 \
  -e POSTGRES_PASSWORD=password \
  timescale/timescaledb:latest-pg18

# 或在现有 PostgreSQL 上安装
git clone https://github.com/timescale/timescaledb.git
cd timescaledb
./bootstrap -DUSE_OPENSSL=OFF
cd build && make install
```

---

### 🔹 对象存储

#### MinIO ⭐ 阶段 2 推荐（S3 兼容）

**官网**: https://min.io/
**GitHub**: https://github.com/minio/minio

**适用场景**:
- ✅ 文件存储（图片、PDF、Excel）
- ✅ S3 API 兼容
- ✅ 私有云部署

**安装**:
```bash
# Docker 快速启动
docker run -d \
  -p 9000:9000 -p 9001:9001 \
  --name minio \
  -e "MINIO_ROOT_USER=admin" \
  -e "MINIO_ROOT_PASSWORD=password123" \
  quay.io/minio/minio server /data --console-address ":9001"
```

**Python 集成**:
```python
from minio import Minio
from minio.error import S3Error

client = Minio(
    "localhost:9000",
    access_key="admin",
    secret_key="password123",
    secure=False
)

# 上传文件
client.fput_object(
    "bdc-assets",
    "projects/123/image.jpg",
    "/path/to/image.jpg"
)
```

---

## 三、AI 与多模态处理

### 🔹 OCR 引擎

#### PaddleOCR ⭐ 已采用

**GitHub**: https://github.com/PaddlePaddle/PaddleOCR
**版本**: 2.7.0.3

**功能**:
- 中英文混合识别
- 80+ 种语言支持
- 平均置信度 > 95%

**安装**:
```bash
pip install paddleocr==2.7.0.3
pip install paddlepaddle==2.6.2
```

**使用示例**:
```python
from paddleocr import PaddleOCR

ocr = PaddleOCR(use_angle_cls=True, lang='ch')
result = ocr.ocr('image.jpg', cls=True)

for line in result:
    text = line[1][0]  # 识别文本
    confidence = line[1][1]  # 置信度
    print(f"{text} ({confidence:.2f})")
```

---

### 🔹 多模态大模型

#### GLM-4V ⭐ 已采用（智谱 AI）

**官网**: https://open.bigmodel.cn/
**GitHub**: https://github.com/THUDM/GLM-4

**适用场景**:
- ✅ 场景问题分析
- ✅ 图片理解与诊断
- ✅ 结构化输出

**替代方案**:
- **Claude 3.5 Sonnet** - 多模态能力更强
- **GPT-4V** - 通用场景理解
- **Qwen-VL** - 阿里开源,可本地部署

---

### 🔹 Agent 编排框架

#### LangChain + LangGraph ⭐ 阶段 2 推荐

**官网**:
- https://www.langchain.com/
- https://langchain-ai.github.io/langgraph/

**适用场景**:
- ✅ 复杂 Agent 工作流
- ✅ 工具链管理
- ✅ 状态机式流程控制

**安装**:
```bash
pip install langchain langchain-openai langgraph
```

**示例（Agent 工作流）**:
```python
from langgraph.graph import StateGraph, END

class AgentState(TypedDict):
    project_id: str
    assets: list
    analysis_result: dict

# 定义工作流
workflow = StateGraph(AgentState)

workflow.add_node("data_prep", prepare_data_node)
workflow.add_node("model_inference", claude_inference_node)
workflow.add_node("rule_check", expert_rule_node)
workflow.add_node("result_summary", summary_node)

workflow.set_entry_point("data_prep")
workflow.add_edge("data_prep", "model_inference")
workflow.add_edge("model_inference", "rule_check")
workflow.add_edge("rule_check", "result_summary")
workflow.add_edge("result_summary", END)
```

---

### 🔹 规则引擎

#### JSONLogic ⭐ 阶段 2 推荐

**官网**: http://jsonlogic.com/
**GitHub**: https://github.com/jwadhams/json-logic-js

**适用场景**:
- ✅ 专家规则配置
- ✅ 动态规则评估
- ✅ 跨语言支持

**Python 使用**:
```python
from jsonlogic import jsonLogic

# 规则: 如果室外温度 > 30°C 且制冷机组能效 < 3.0
rule = {
    "and": [
        {">": [{"var": "outdoor_temp"}, 30]},
        {"<": [{"var": "chiller_efficiency"}, 3.0]}
    ]
}

data = {
    "outdoor_temp": 32,
    "chiller_efficiency": 2.8
}

result = jsonLogic(rule, data)  # 返回 True
```

---

## 四、前端与移动端技术

### 🔹 PC 前端框架

#### React + Ant Design ⭐ 阶段 3 推荐

**官网**:
- https://react.dev/
- https://ant.design/

**适用场景**:
- ✅ 数据管理后台
- ✅ 复杂表格和图表
- ✅ 企业级 UI 组件

**快速开始**:
```bash
# 使用 Vite
npm create vite@latest bdc-ai-admin -- --template react-ts
cd bdc-ai-admin
npm install antd
npm run dev
```

**替代方案**:
- **Vue 3 + Element Plus** - Vue 技术栈
- **Streamlit** - Python 原生快速原型

---

### 🔹 移动端开发框架

#### React Native ⭐ 推荐（跨平台 App）

**官网**: https://reactnative.dev/
**GitHub**: https://github.com/facebook/react-native

**适用场景**:
- ✅ iOS + Android 原生 App
- ✅ 团队熟悉 React/JavaScript
- ✅ 丰富的第三方库

**核心库推荐**:
```bash
# 使用 Expo（最简单）
npx create-expo-app bdc-ai-app

# 安装核心依赖
npm install react-native-paper @tanstack/react-query
npm install expo-camera expo-location expo-av
npm install @react-native-async-storage/async-storage
```

**技术栈组合**:
```
框架: React Native + Expo
语言: TypeScript
UI: React Native Paper
状态管理: Zustand
网络请求: Axios + React Query
本地存储: AsyncStorage + SQLite
相机: expo-camera
语音识别: 在线 ASR API（百度/阿里）
定位: expo-location
```

---

#### uni-app ⭐ 推荐（小程序优先）

**官网**: https://uniapp.dcloud.net.cn/
**GitHub**: https://github.com/dcloudio/uni-app

**适用场景**:
- ✅ 微信/支付宝小程序
- ✅ 一套代码多端运行
- ✅ 团队熟悉 Vue.js

**支持平台**:
- ✅ 微信小程序
- ✅ 支付宝小程序
- ✅ App（iOS + Android）
- ✅ H5

**快速开始**:
```bash
# 使用 HBuilderX 创建
# 或使用 CLI
npx @dcloudio/uvm create bdc-ai-miniprogram
cd bdc-ai-miniprogram
npm run dev:mp-weixin
```

---

#### Flutter ⭐ 推荐（性能优先）

**官网**: https://flutter.dev/
**GitHub**: https://github.com/flutter/flutter

**适用场景**:
- ✅ 追求高性能和流畅体验
- ✅ 团队熟悉 Dart
- ✅ 自定义 UI 效果

**快速开始**:
```bash
flutter create bdc_ai_app
cd bdc_ai_app
flutter pub add dio provider camera
flutter run
```

---

### 🔹 移动端选型决策树

```
团队有 React 经验？
├─ 是 → 需要小程序？
│   ├─ 是 → Taro（React 技术栈）
│   └─ 否 → React Native + Expo
└─ 否 → 团队熟悉 Vue？
    ├─ 是 → uni-app（多端）
    └─ 否 → Flutter（性能优先）
```

---

## 五、向量数据库与检索

### 🔹 pgvector ⭐ 阶段 2 推荐（PostgreSQL 扩展）

**GitHub**: https://github.com/pgvector/pgvector

**适用场景**:
- ✅ 向量相似度搜索
- ✅ 多模态语义检索
- ✅ 与现有 PostgreSQL 无缝集成

**安装**:
```bash
# PostgreSQL 18.1 + pgvector
git clone --branch v0.7.4 https://github.com/pgvector/pgvector.git
cd pgvector
make
make install # 可能需要 sudo
```

**启用扩展**:
```sql
CREATE EXTENSION vector;

-- 创建向量列
CREATE TABLE items (
  id bigserial PRIMARY KEY,
  content text,
  embedding vector(1536)  -- OpenAI embedding 维度
);

-- 插入向量
INSERT INTO items (content, embedding)
VALUES ('hello', '[0.1, 0.2, ...]');

-- 相似度搜索
SELECT * FROM items
ORDER BY embedding <-> '[0.1, 0.2, ...]'
LIMIT 5;
```

---

### 🔹 Qdrant ⭐ 独立向量库推荐

**官网**: https://qdrant.tech/
**GitHub**: https://github.com/qdrant/qdrant

**适用场景**:
- ✅ 高性能向量检索
- ✅ 独立部署
- ✅ 丰富的过滤功能

**安装**:
```bash
# Docker 快速启动
docker run -d -p 6333:6333 -p 6334:6334 \
  --name qdrant \
  qdrant/qdrant:latest
```

**Python 集成**:
```python
from qdrant_client import QdrantClient
from qdrant_client.models import Distance, VectorParams, PointStruct

client = QdrantClient("localhost", port=6333)

# 创建集合
client.create_collection(
    collection_name="bdc_assets",
    vectors_config=VectorParams(size=1536, distance=Distance.COSINE)
)

# 插入向量
client.upsert(
    collection_name="bdc_assets",
    points=[
        PointStruct(id=1, vector=[0.1, 0.2, ...], payload={"city": "北京"})
    ]
)

# 搜索
results = client.search(
    collection_name="bdc_assets",
    query_vector=[0.1, 0.2, ...],
    limit=5
)
```

---

## 六、推荐的扩展技术

### 🔹 多模数据处理

#### LlamaIndex ⭐ 推荐（RAG 框架）

**官网**: https://www.llamaindex.ai/
**GitHub**: https://github.com/run-llama/llama_index

**适用场景**:
- ✅ 多模态 RAG
- ✅ 文档解析（PDF、表格）
- ✅ 向量检索集成

**安装**:
```bash
pip install llama-index-core llama-index-vector-stores-qdrant
```

---

### 🔹 文档解析

#### Unstructured.io ⭐ 阶段 2 推荐

**官网**: https://unstructured.io/
**GitHub**: https://github.com/Unstructured-IO/unstructured

**适用场景**:
- ✅ PDF 解析
- ✅ 表格提取
- ✅ 多模态文档处理

**安装**:
```bash
pip install "unstructured[local-inference]"
pip install "unstructured[all-docs]"
```

**使用示例**:
```python
from unstructured.partition.pdf import partition_pdf

elements = partition_pdf(
    filename="energy_report.pdf",
    strategy="hi_res",  # 高精度模式
    extract_images_in_pdf=True,
    infer_table_structure=True
)

for element in elements:
    print(f"{element.category}: {element.text}")
```

---

### 🔹 时序数据可视化

#### Grafana ⭐ 推荐（监控大屏）

**官网**: https://grafana.com/
**GitHub**: https://github.com/grafana/grafana

**适用场景**:
- ✅ 能耗数据可视化
- ✅ 实时监控大屏
- ✅ 告警规则配置

**安装**:
```bash
# Docker 快速启动
docker run -d \
  -p 3000:3000 \
  --name grafana \
  grafana/grafana:latest
```

---

## 📊 技术栈演进路线

### 阶段 1 MVP ✅ 已完成
```
后端: FastAPI + SQLAlchemy + PostgreSQL
AI: PaddleOCR + GLM-4V
存储: 本地文件系统
树形结构: bigtree
```

### 阶段 2: 多模态与专家库（下一步）
```
向量检索: pgvector / Qdrant
Agent 编排: LangGraph
规则引擎: JSONLogic
对象存储: MinIO
时序数据: TimescaleDB
文档解析: Unstructured.io
```

### 阶段 3: 优化与扩展
```
PC 前端: React + Ant Design
移动端: React Native / uni-app
可视化: Grafana
缓存: Redis
消息队列: Celery / RabbitMQ
```

---

## 🎯 技术选型建议

### 后端技术
- **Web 框架**: FastAPI（已采用）✅
- **数据库**: PostgreSQL 18.1（已采用）✅
- **ORM**: SQLAlchemy 2.0（已采用）✅
- **时序扩展**: TimescaleDB（阶段 2）⭐
- **对象存储**: MinIO（阶段 2）⭐

### AI 与多模态
- **OCR**: PaddleOCR（已采用）✅
- **多模态大模型**: GLM-4V / Claude（已采用）✅
- **Agent 编排**: LangGraph（阶段 2）⭐
- **规则引擎**: JSONLogic（阶段 2）⭐

### 前端与移动端
- **PC 前端**: React + Ant Design（阶段 3）⭐
- **移动端**: React Native / uni-app（阶段 3）⭐

### 向量与检索
- **向量库**: pgvector / Qdrant（阶段 2）⭐
- **RAG 框架**: LlamaIndex（可选）

---

## 📚 相关文档

- **项目规划.md** - 详细的项目规划
- **技术指南.md** - 深入的技术解析
- **工程结构API设计.md** - API 设计文档
- **docs/API使用示例.md** - API 使用示例

---

**文档维护**: BDC-AI 开发团队
**最后更新**: 2026-01-20
**文档版本**: v2.0
