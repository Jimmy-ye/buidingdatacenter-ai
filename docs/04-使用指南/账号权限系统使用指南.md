# BDC-AI 账号权限系统使用指南

最后更新：2026-01-25

---

## 📋 目录

1. [快速开始](#快速开始)
2. [Web API 使用](#web-api-使用)
3. [Python 脚本调用](#python-脚本调用)
4. [移动端集成](#移动端集成)
5. [PC 端集成](#pc-端集成)
6. [用户和权限管理](#用户和权限管理)
7. [常见场景](#常见场景)

---

## 快速开始

### 默认管理员账号

```
用户名：admin
邮箱：admin@bdc-ai.com
密码：admin123
角色：超级管理员（Super Administrator）
```

⚠️ **安全提示**：生产环境请立即修改默认密码！

---

## Web API 使用

### 1. 使用 Swagger UI（最简单）

**访问地址**：http://localhost:8000/docs

#### 步骤 1：登录获取 Token

1. 打开 http://localhost:8000/docs
2. 找到 `/api/v1/auth/login` 端点
3. 点击 "Try it out"
4. 输入凭证：
   ```json
   {
     "username": "admin",
     "password": "admin123"
   }
   ```
5. 点击 "Execute"
6. 从响应中复制 `access_token`

#### 步骤 2：设置认证

1. 点击页面右上角的 🔓 "Authorize" 按钮
2. 在弹出框中输入：`Bearer 你的access_token`
   ```
   Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   ```
3. 点击 "Authorize"
4. 关闭对话框

#### 步骤 3：调用受保护的 API

现在你可以调用任何需要认证的 API 了：
- 获取项目列表
- 创建项目
- 查看用户信息
- 管理角色权限

---

### 2. 使用 cURL

#### 登录

```bash
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin123"
  }'
```

**响应**：
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "refresh_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```

#### 保存 Token

```bash
# Linux/Mac
export TOKEN="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# Windows PowerShell
$TOKEN="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# Windows CMD
set TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

#### 使用 Token 访问 API

```bash
# 获取当前用户信息
curl http://localhost:8000/api/v1/auth/me \
  -H "Authorization: Bearer $TOKEN"

# 获取项目列表
curl http://localhost:8000/api/v1/projects/ \
  -H "Authorization: Bearer $TOKEN"

# 创建项目
curl -X POST http://localhost:8000/api/v1/projects/ \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "测试项目",
    "client": "测试客户",
    "type": "commercial",
    "status": "planning"
  }'
```

---

## Python 脚本调用

### 创建 API 客户端类

```python
"""
BDC-AI API 客户端示例
"""
import requests
from typing import Optional

class BDCAClient:
    """BDC-AI API 客户端"""

    def __init__(self, base_url: str = "http://localhost:8000"):
        self.base_url = base_url
        self.token: Optional[str] = None
        self.user_info: Optional[dict] = None

    def login(self, username: str, password: str) -> bool:
        """登录"""
        response = requests.post(
            f"{self.base_url}/api/v1/auth/login",
            json={"username": username, "password": password}
        )

        if response.status_code == 200:
            data = response.json()
            self.token = data["access_token"]
            self.user_info = data.get("user", {})
            print(f"✓ 登录成功：{self.user_info.get('username')}")
            return True
        else:
            print(f"✗ 登录失败：{response.status_code}")
            return False

    def get_headers(self) -> dict:
        """获取认证头"""
        if not self.token:
            raise ValueError("未登录，请先调用 login()")
        return {"Authorization": f"Bearer {self.token}"}

    def get_current_user(self) -> dict:
        """获取当前用户信息"""
        response = requests.get(
            f"{self.base_url}/api/v1/auth/me",
            headers=self.get_headers()
        )
        response.raise_for_status()
        return response.json()

    def get_projects(self, **filters) -> list:
        """获取项目列表"""
        response = requests.get(
            f"{self.base_url}/api/v1/projects/",
            headers=self.get_headers(),
            params=filters
        )
        response.raise_for_status()
        return response.json()

    def create_project(self, project_data: dict) -> dict:
        """创建项目"""
        response = requests.post(
            f"{self.base_url}/api/v1/projects/",
            headers=self.get_headers(),
            json=project_data
        )
        response.raise_for_status()
        return response.json()

    def update_project(self, project_id: str, project_data: dict) -> dict:
        """更新项目"""
        response = requests.patch(
            f"{self.base_url}/api/v1/projects/{project_id}",
            headers=self.get_headers(),
            json=project_data
        )
        response.raise_for_status()
        return response.json()

    def delete_project(self, project_id: str) -> None:
        """删除项目"""
        response = requests.delete(
            f"{self.base_url}/api/v1/projects/{project_id}",
            headers=self.get_headers()
        )
        response.raise_for_status()

# 使用示例
if __name__ == "__main__":
    # 创建客户端
    client = BDCAClient()

    # 登录
    if client.login("admin", "admin123"):
        # 获取用户信息
        user = client.get_current_user()
        print(f"用户信息：{user}")

        # 获取项目列表
        projects = client.get_projects()
        print(f"项目数量：{len(projects)}")

        # 创建新项目
        new_project = client.create_project({
            "name": "示例项目",
            "client": "示例客户",
            "type": "commercial",
            "status": "planning",
            "description": "这是一个测试项目"
        })
        print(f"创建的项目：{new_project}")
```

### 简单脚本示例

```python
import requests

# 1. 登录
login_response = requests.post(
    "http://localhost:8000/api/v1/auth/login",
    json={"username": "admin", "password": "admin123"}
)
token = login_response.json()["access_token"]

# 2. 使用 Token 访问 API
headers = {"Authorization": f"Bearer {token}"}

# 获取项目列表
projects = requests.get(
    "http://localhost:8000/api/v1/projects/",
    headers=headers
).json()

print(f"找到 {len(projects)} 个项目")
for project in projects:
    print(f"  - {project['name']} ({project['status']})")

# 创建新项目
new_project = requests.post(
    "http://localhost:8000/api/v1/projects/",
    headers=headers,
    json={
        "name": "新项目",
        "client": "客户A",
        "type": "commercial",
        "status": "planning"
    }
).json()

print(f"创建项目成功：{new_project['id']}")
```

---

## 移动端集成

### Flutter 集成示例

#### 1. 添加依赖

```yaml
# pubspec.yaml
dependencies:
  flutter_secure_storage: ^8.0.0
  http: ^1.1.0
  shared_preferences: ^2.2.0
```

#### 2. 创建认证服务

```dart
// lib/services/auth_service.dart
import 'dart:convert';
import 'package:http/http.dart' as http;
import 'package:flutter_secure_storage/flutter_secure_storage.dart';
import 'package:shared_preferences/shared_preferences.dart';

class AuthService {
  final String baseUrl;
  final FlutterSecureStorage _storage = const FlutterSecureStorage();

  String? _accessToken;
  String? _refreshToken;

  AuthService({this.baseUrl = 'http://localhost:8000'});

  // 登录
  Future<bool> login(String username, String password) async {
    final response = await http.post(
      Uri.parse('$baseUrl/api/v1/auth/login'),
      headers: {'Content-Type': 'application/json'},
      body: jsonEncode({
        'username': username,
        'password': password,
      }),
    );

    if (response.statusCode == 200) {
      final data = jsonDecode(response.body);
      _accessToken = data['access_token'];
      _refreshToken = data['refresh_token'];

      // 保存 Token
      await _storage.write(key: 'access_token', value: _accessToken);
      await _storage.write(key: 'refresh_token', value: _refreshToken);

      return true;
    }

    return false;
  }

  // 获取认证头
  Map<String, String> getAuthHeaders() {
    final token = _accessToken ?? '';
    return {
      'Content-Type': 'application/json',
      'Authorization': 'Bearer $token',
    };
  }

  // 获取项目列表
  Future<List<dynamic>> getProjects() async {
    final response = await http.get(
      Uri.parse('$baseUrl/api/v1/projects/'),
      headers: getAuthHeaders(),
    );

    if (response.statusCode == 200) {
      return jsonDecode(response.body);
    }

    throw Exception('获取项目列表失败');
  }

  // 登出
  Future<void> logout() async {
    await _storage.delete(key: 'access_token');
    await _storage.delete(key: 'refresh_token');
    _accessToken = null;
    _refreshToken = null;
  }
}
```

#### 3. 使用示例

```dart
// lib/screens/home_screen.dart
import 'package:flutter/material.dart';
import '../services/auth_service.dart';

class HomeScreen extends StatefulWidget {
  @override
  _HomeScreenState createState() => _HomeScreenState();
}

class _HomeScreenState extends State<HomeScreen> {
  final AuthService _authService = AuthService();
  List<dynamic> _projects = [];
  bool _loading = false;

  @override
  void initState() {
    super.initState();
    _loadProjects();
  }

  Future<void> _loadProjects() async {
    setState(() => _loading = true);

    try {
      final projects = await _authService.getProjects();
      setState(() => _projects = projects);
    } catch (e) {
      print('错误：$e');
    } finally {
      setState(() => _loading = false);
    }
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('BDC-AI'),
        actions: [
          IconButton(
            icon: Icon(Icons.logout),
            onPressed: () async {
              await _authService.logout();
              Navigator.pushReplacementNamed(context, '/login');
            },
          ),
        ],
      ),
      body: _loading
          ? Center(child: CircularProgressIndicator())
          : ListView.builder(
              itemCount: _projects.length,
              itemBuilder: (context, index) {
                final project = _projects[index];
                return ListTile(
                  title: Text(project['name']),
                  subtitle: Text(project['client']),
                  trailing: Text(project['status']),
                );
              },
            ),
    );
  }
}
```

---

## PC 端集成

### NiceGUI 集成示例

```python
"""
BDC-AI PC 端 - NiceGUI 集成示例
"""
from nicegui import ui, app
import requests

API_BASE = "http://localhost:8000"

class AppState:
    """应用状态"""
    def __init__(self):
        self.token = None
        self.user = None

state = AppState()

def login_page():
    """登录页面"""
    ui.label('BDC-AI 建筑节能管理平台').classes('text-3xl font-bold')

    with ui.card().classes('w-96'):
        username = ui.input('用户名', placeholder='admin')
        password = ui.input('密码', password=True, placeholder='admin123')

        async def do_login():
            try:
                response = requests.post(
                    f"{API_BASE}/api/v1/auth/login",
                    json={
                        "username": username.value,
                        "password": password.value
                    }
                )

                if response.status_code == 200:
                    data = response.json()
                    state.token = data['access_token']
                    state.user = data.get('user')

                    # 保存到 storage
                    app.storage.user['token'] = state.token

                    ui.notify('登录成功', type='positive')
                    main_page()
                else:
                    ui.notify('登录失败，请检查用户名和密码', type='negative')
            except Exception as e:
                ui.notify(f'登录错误：{e}', type='negative')

        ui.button('登录', on_click=do_login).classes('w-full mt-4')

def get_headers():
    """获取认证头"""
    return {"Authorization": f"Bearer {state.token}"}

def main_page():
    """主页面"""
    ui.query('body').classes('bg-gray-50')

    with ui.header().classes('bg-blue-600 text-white p-4'):
        ui.label(f'BDC-AI - 欢迎 {state.user["username"]}').classes('text-xl font-bold')

        ui.button(on_click=lambda: ui.navigate('/login')) \
            .props('outline round').classes('ml-auto')

    with ui.column().classes('p-4 w-full max-w-6xl mx-auto'):
        ui.label('项目管理').classes('text-2xl font-bold mb-4')

        # 项目列表
        async def load_projects():
            try:
                response = requests.get(
                    f"{API_BASE}/api/v1/projects/",
                    headers=get_headers()
                )
                projects = response.json()

                with ui.card().classes('w-full'):
                    ui.label(f'共 {len(projects)} 个项目').classes('text-lg font-bold')

                    for project in projects:
                        with ui.card().classes('mb-2'):
                            ui.label(project['name']).classes('text-lg font-bold')
                            ui.label(f"客户：{project['client']}").classes('text-gray-600')
                            ui.label(f"状态：{project['status']}").classes('text-sm')

            except Exception as e:
                ui.notify(f'加载失败：{e}', type='negative')

        load_projects()

# 初始化
@ui.page('/')
def index():
    # 检查是否已登录
    if app.storage.user.get('token'):
        state.token = app.storage.user['token']
        main_page()
    else:
        login_page()

# 启动应用
ui.run(port=8080, title='BDC-AI 管理平台')
```

---

## 用户和权限管理

### 1. 创建新用户（仅管理员）

```bash
# 使用管理员 Token
export ADMIN_TOKEN="..."

# 创建新用户
curl -X POST http://localhost:8000/api/v1/auth/users/ \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "engineer1",
    "email": "engineer1@example.com",
    "password": "Pass123!@#",
    "is_active": true,
    "is_superuser": false
  }'
```

### 2. 为用户分配角色

```bash
# 为用户分配角色
USER_ID="用户UUID"
ROLE_ID="角色UUID"

curl -X POST http://localhost:8000/api/v1/auth/users/{user_id}/roles \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"role_id": "$ROLE_ID"}'
```

### 3. 查看所有角色和权限

```bash
# 获取角色列表
curl http://localhost:8000/api/v1/auth/roles/ \
  -H "Authorization: Bearer $TOKEN"

# 获取权限列表
curl http://localhost:8000/api/v1/auth/permissions/ \
  -H "Authorization: Bearer $TOKEN"
```

### 4. 修改密码

```bash
# 修改当前用户密码
curl -X POST http://localhost:8000/api/v1/auth/me/change-password \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "old_password": "admin123",
    "new_password": "NewSecurePass456!"
  }'

# 管理员修改其他用户密码
curl -X POST http://localhost:8000/api/v1/auth/users/{user_id}/reset-password \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"new_password": "NewPass123!"}'
```

---

## 常见场景

### 场景 1：日常开发测试

```python
import requests

# 使用测试账号登录
response = requests.post(
    "http://localhost:8000/api/v1/auth/login",
    json={"username": "admin", "password": "admin123"}
)
token = response.json()["access_token"]

# 在代码中使用
headers = {"Authorization": f"Bearer {token}"}

# 测试你的 API
requests.get("http://localhost:8000/api/v1/projects/", headers=headers)
```

### 场景 2：自动化脚本

```python
"""
自动化数据导入脚本
"""
import requests
import csv

class BDC_AImporter:
    def __init__(self):
        self.client = requests.Session()
        self.token = None

    def login(self, username, password):
        """登录"""
        response = self.client.post(
            "http://localhost:8000/api/v1/auth/login",
            json={"username": username, "password": password}
        )
        self.token = response.json()["access_token"]
        self.client.headers.update({
            "Authorization": f"Bearer {self.token}"
        })

    def import_projects(self, csv_file):
        """从 CSV 导入项目"""
        with open(csv_file) as f:
            reader = csv.DictReader(f)
            for row in reader:
                self.client.post(
                    "http://localhost:8000/api/v1/projects/",
                    json=row
                )
                print(f"✓ 导入项目：{row['name']}")

# 使用
importer = BDC_AImporter()
importer.login("admin", "admin123")
importer.import_projects("projects.csv")
```

### 场景 3：多环境部署

```python
import requests
import os

class Config:
    DEV = "http://localhost:8000"
    STAGING = "http://staging.example.com"
    PRODUCTION = "https://api.example.com"

# 根据环境变量选择 API 地址
API_BASE = os.getenv("BDC_API_URL", Config.DEV)

def get_client():
    """获取已认证的客户端"""
    client = requests.Session()

    # 从环境变量获取凭证
    username = os.getenv("BDC_USERNAME")
    password = os.getenv("BDC_PASSWORD")

    # 登录
    response = client.post(
        f"{API_BASE}/api/v1/auth/login",
        json={"username": username, "password": password}
    )
    token = response.json()["access_token"]

    # 设置认证头
    client.headers.update({"Authorization": f"Bearer {token}"})

    return client
```

---

## 🚀 下一步

1. **修改默认密码**：
   ```bash
   curl -X POST http://localhost:8000/api/v1/auth/me/change-password \
     -H "Authorization: Bearer $TOKEN" \
     -H "Content-Type: application/json" \
     -d '{"old_password":"admin123","new_password":"你的新密码"}'
   ```

2. **访问 API 文档**：http://localhost:8000/docs

3. **查看认证测试报告**：`AUTH_TEST_REPORT.md`

4. **阅读更多文档**：
   - 后端部署指南：`docs/02-技术文档/后端服务器部署完整指南.md`
   - 安全审查报告：`docs/03-优化记录/2026-01-24-账号权限系统安全审查.md`

---

**文档维护**：BDC-AI 开发团队
**最后更新**：2026-01-25
**状态**：✅ 已完成
