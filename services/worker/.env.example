# GLM-4V Scene Issue Worker 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ================= 后端配置 =================
# 后端服务地址（FastAPI）
BDC_BACKEND_BASE_URL=http://127.0.0.1:8000

# 本地存储目录（必须与 shared/config/settings.py 中的 local_storage_dir 一致）
BDC_LOCAL_STORAGE_DIR=d:\Huawei Files\华为家庭存储\Programs\program-bdc-ai\data\local_storage

# 可选：仅处理指定项目的 scene_issue 资产（留空则处理所有项目）
# BDC_SCENE_PROJECT_ID=your-project-uuid-here

# Worker 轮询间隔（秒），默认 60
BDC_SCENE_WORKER_POLL_INTERVAL=600

# ================= GLM API 配置 =================
# GLM API Key（从 https://open.bigmodel.cn/ 获取）
GLM_API_KEY=your-glm-api-key-here

# 可选：GLM API 地址（默认已设置）
# GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4/

# 可选：GLM 视觉模型（默认 glm-4v）
# GLM_VISION_MODEL=glm-4v
