# BDC-AI 环境变量配置模板
# 复制此文件为 .env 并填入实际配置值

# ================================
# 数据库配置
# ================================
# PostgreSQL 数据库连接 URL
BDC_DATABASE_URL=postgresql://admin:password@localhost:5432/bdc_ai

# 本地文件存储目录（相对于项目根目录）
BDC_LOCAL_STORAGE_DIR=data/assets

# ================================
# GLM API 配置（场景问题分析）
# ================================
# GLM API Key（从智谱 AI 开放平台获取）
# 获取方式：https://open.bigmodel.cn/
GLM_API_KEY=your_glm_api_key_here

# GLM API 基础 URL（通常不需要修改）
GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4/

# GLM 视觉模型（通常不需要修改）
GLM_VISION_MODEL=glm-4v

# GLM Worker 轮询间隔（秒）
BDC_SCENE_WORKER_POLL_INTERVAL=300

# 可选：仅处理特定项目的场景问题（留空则处理所有项目）
# BDC_SCENE_PROJECT_ID=

# ================================
# MinIO 对象存储（可选，生产环境推荐）
# ================================
# MinIO 服务地址
# BDC_MINIO_ENDPOINT=localhost:9000

# MinIO 访问密钥
# BDC_MINIO_ACCESS_KEY=minioadmin

# MinIO 秘密密钥
# BDC_MINIO_SECRET_KEY=minioadmin

# MinIO 存储桶名称
# BDC_MINIO_BUCKET=bdc-assets

# ================================
# 后端服务配置
# ================================
# 后端服务基础 URL（Worker 连接后端使用）
# BDC_BACKEND_BASE_URL=http://localhost:8000

# ================================
# PC-UI 配置
# ================================
# 环境类型（development, testing, production）
ENVIRONMENT=development

# PC-UI 后端 API 地址（可选，默认使用 http://127.0.0.1:8000/api/v1）
# BDC_API_URL=http://127.0.0.1:8000/api/v1

# Tailscale 远程访问示例：
# BDC_API_URL=http://100.93.101.76:8000/api/v1

# ================================
# 开发环境配置
# ================================
# DEBUG 模式（开发环境可设为 true）
# DEBUG=false

# 日志级别（DEBUG, INFO, WARNING, ERROR, CRITICAL）
# LOG_LEVEL=INFO
